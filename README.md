# ImageEval 2026 Website

This is a Jekyll-style static site for the ImageEval Shared Task 2026.

Run locally:

```bash
bundle install
bundle exec jekyll serve
```

Then open `http://localhost:4000/`.
